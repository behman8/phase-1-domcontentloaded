document.addEventListener("DOMContentLoaded", (event) => {
    const element = document.getElementById("text");
    const textChange = element.innerHTML = "This is really cool!";
    return event = textChange;
});